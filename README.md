1. Clone application to your computer
2. Open a folder where application was cloned
3. Install all dependancies `npm install`
4. To run application `npm run start`
5. Open browser with adress `http://localhost:8080/`